#!/bin/sh
set -e

case "$1" in
	configure)
		# First install: create the well-known directories inside the
		# virtual file root.
		mkdir -p /var/lib/aun-filestore-root/LIBRARY
		mkdir -p /var/lib/aun-filestore-root/HOME/SYST
		;;
esac

#DEBHELPER#

exit 0
